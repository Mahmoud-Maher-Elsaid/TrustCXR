# Stage 17B Rule-Based Research-Triage Contract

The only currently activated outcome is deterministic `DEFER`. Input rejection has no accepted automated signal, and review-priority mappings remain withheld pending an expert-approved finding-priority policy. Precedence is `INPUT_REJECTED > DEFER > REVIEW_PRIORITY`; inactive decisions cannot fire. Every active rule has a reason code. This is research triage only and provides no treatment advice or clinical deployment claim.
