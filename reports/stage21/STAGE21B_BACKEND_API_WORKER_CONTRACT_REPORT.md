# Stage 21B Backend API and GPU Worker Contract

Versioned deterministic schemas, a DEFER-aware job state machine, bounded GPU worker messages, sanitized failure semantics, request isolation, and frozen orchestration limits are defined. No server, worker, serving dependency, model inference, real patient processing, or language-model endpoint was started or added.
